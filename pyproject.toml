[project]
name = "srtp-code-llm-pruning"
version = "0.1.0"
description = "Stage 1 reproduction workspace for code LLM pruning methods"
requires-python = ">=3.11,<3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
