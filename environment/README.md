# Environment

The experiments run under WSL2 with Python virtual environments. WSL2 is the execution environment, not a repository source boundary. Dependency locks are stored in `environment/locks/`; `environment/method_env_map.csv` maps methods to environments. Large artifacts belong under `SRTP_ARTIFACT_ROOT`.
