# Environment Snapshots

This directory stores dependency snapshots for each virtual environment.

The actual virtual environments live at repository root as `.venv-*` directories and are ignored by Git.

Use these files for reproducibility checks, not as runtime code.
