# R4-Half Formal Results

This table is generated from `results/status/scores.csv`. Included rows have `protocol=r4_half`, `result_completeness` of `complete` or `partial`, and are not invalid. Pilot rows remain in the status registry but are excluded here. Corrected Qwen dense baselines supersede older baseline rows. Partial rows keep their actual task counts.
