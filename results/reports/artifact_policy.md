# Artifact Policy

Large model weights and generated model directories are stored outside Git under `SRTP_ARTIFACT_ROOT`. The artifact registry records locator, availability, size when known, and Git policy.
