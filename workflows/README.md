# Workflows

Cross-method workflow code lives here:

- `data/`: split and manifest builders.
- `evaluate/`: the single active generation and scoring pipeline.
- `recovery/`: distillation data, LoRA training, and LoRA merge helpers.
- `experiment/`: plan runner.
- `audit/`: static and evidence audit tools.

