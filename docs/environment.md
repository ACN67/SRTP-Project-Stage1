# Environment Protocol

Use Python 3.11 virtual environments. Shared pruning locks are consolidated in `environment/locks/shared_pruning.txt`; method-specific locks remain separate. Generated caches, compiled Python files, model weights, and heavy run artifacts are excluded from the active repository.
