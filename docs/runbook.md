# Runbook

Run checks with `python -m pytest -q`, `python -m compileall -q methods workflows tests`, shell syntax checks for `workflows` and `environment/setup`, and `git diff --check`. Experiment jobs write to `results/evidence/<category>/<run_id>`.
