# Stage 1 Protocol

Stage 1 evaluates whether 12 pruning methods can be adapted to code-model reproduction workflows with traceable evidence. Rounds cover environment preparation, upstream smoke checks, adapter validation, recovery, formal R4-half evaluation, and registry generation. Raw evidence is immutable; status corrections are represented in `results/status/`.
