# Benchmark Protocol

The active evaluator is `workflows/evaluate/run.sh`, backed by `generate.py` and the benchmark-specific scorers. R4-half uses split pairs under `data/benchmarks/r4_half/`. The auxiliary full-evaluation protocol uses data under `data/benchmarks/auxiliary_full_eval/` and is not directly comparable with R4-half.
